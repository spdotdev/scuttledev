{
  "name": "scuttledev",
  "description": "",
  "private": true,
  "version": "1.0.0",
  "main": "vite.config.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "generate-pdfs": "node scripts/generate-pdfs.js",
    "build": "npm run generate-pdfs && vite build",
    "preview": "vite preview"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/spdotdev/splotnikov.git"
  },
  "devDependencies": {
    "md-to-pdf": "^5.2.5",
    "vite": "^5.0.0"
  }
}
